org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Sideload / CI signing (override via env in production if needed)
KEEPSAKE_STORE_PASSWORD=keepsake-ci
KEEPSAKE_KEY_PASSWORD=keepsake-ci
KEEPSAKE_KEY_ALIAS=keepsake
